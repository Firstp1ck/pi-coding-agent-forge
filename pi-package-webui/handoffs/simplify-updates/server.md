# Worker A bounded review-fix handoff — parent inspection requested

HEAD `28252dfc96a9c41a7095159c63e46b82b24a023f`. This continuation addressed accepted E1–E8, U3, U6 without changing UI, versions, dependencies, publishing, or live Pi/npm installations. No commits or staging performed by this continuation. Existing staged files are **pre-existing**; do not mistake `git diff --cached` for this pass's worktree delta. Stop writing before parent inspection.

## Changes

- E1: server registration now includes independently resolved active Pi package root even when explicit selection differs from PATH Pi; startup registration precedes optional-feature mutation (E7). The two-root participant/lease fixture verifies overlap and distinct ownership.
- E2: ordinary `/api/restart` reserves counted admission before its first await, rechecks the fence immediately before successor launch and releases the lease afterward. Lock/lease regression verifies no idle acknowledgment during a suspended restart and rejection after fencing.
- E3: npm-reported actual `userconfig`/`globalconfig`, project `.npmrc`, and Pi agent npm config are bound in the plan; relevant scoped registries and environment routing keys join the fingerprint. Runner checks these exact paths on execution. Scoped and redirected-config drift fixtures verify mismatch.
- E4/U1: migration revalidates later equal/newer candidates against the prior accepted version floor, atomically advances its marker, retains original backups and rejects downgrade. Added subsequent-upgrade test.
- E5/U2: actual Pi replacement and WebUI successor require bounded `get_state`, live RPC child and matching session file before success and fence release. A failed/mismatched RPC state throws and leaves `unknown`/exclusion. Added readiness/outcome unit tests and a full isolated fake plan → apply → receipt → verify → fenced Pi restart → healthy restored tab lifecycle test. Fixed restart marker verification to use durable authorized job while its owner-held restart claim causes inspection to report `restart-in-progress`.
- E6: server RPC wrapper synchronously reserves dispatch order before asynchronous admission; commands release wrapper ordering after dispatch rather than waiting for the full RPC response. Existing host FIFO admission remains intact.
- E8/U5: only real failed/unverified targets produce partial; healthy+unchanged is success. Added outcome matrix.
- U3: proven pre-spawn failure releases the lock and persists a terminal failed record, while ambiguous post-spawn failure stays excluded. On Windows the private capsule now copies a detached Node guardian, which starts ordinary hidden verified PowerShell and waits for it. This is necessary because detached PowerShell silently exits before executing `-Command` on this host. The guardian writes a fsynced no-shell-started marker only after a proven spawn error; original owner finalizes that specific failure as terminal after releasing the lock. Missing/uncertain evidence still remains excluded. The crash-survival fixture abruptly exits the initiator (88), then proves that real PowerShell executes the fake command, durable changed receipt/completion survive and exclusion is retained until verified release. The full lifecycle fixture also passes on this host.
- U6: discovery's launched-record budget excludes planned and unrelated previews; test persists 1,025 unconfirmed records without disabling relevant history. A malformed applicable record still fails closed.

Files changed by this pass: `bin/pi-webui.mjs`, `bin/pi-webui-update-context.mjs`, `bin/pi-webui-native-update-runner.mjs`, `bin/pi-webui-native-update-guardian.mjs` (new), `lib/update/native-jobs.mjs`, `lib/update/discovery.mjs`, `lib/update/migration.mjs`, `lib/update/verification.mjs` (new), `tests/native-update-discovery.test.mjs`, `tests/native-update-verification.test.mjs`, `tests/native-update-lifecycle.test.mjs`, `tests/native-update-shell-survival.test.mjs`, `tests/update-plan.test.mjs`, `tests/update-lock-multiprocess-harness.test.mjs`, `tests/update-supervisor-harness.test.mjs`, `tests/update-executor-process-tree-harness.test.mjs`, `tests/update-api-harness.test.mjs`, `tests/http-endpoints-harness.test.mjs`, `DEVELOPMENT.md`, and this handoff. Parent's host FIFO and coordination-home precedence fixes remain intact. B-owned UI/docs/test changes remain intact.

## Validation

- Passing together on current Windows host: `node tests/update-lock-multiprocess-harness.test.mjs`, `node tests/update-plan.test.mjs`, `node tests/native-update-shell-survival.test.mjs`, `node tests/native-update-lifecycle.test.mjs`, `node tests/update-api-harness.test.mjs`, `node tests/rpc-supervisor-host.test.mjs`, `node tests/component-update-api-static.test.mjs`, `node tests/native-update-discovery.test.mjs`, `node tests/native-update-verification.test.mjs`, `node tests/update-supervisor-harness.test.mjs`, `node tests/update-executor-process-tree-harness.test.mjs`. Also `node --check bin/pi-webui.mjs`, `node --check bin/pi-webui-native-update-guardian.mjs`, and mandatory markdown diff check passed.
- `node tests/http-endpoints-harness.test.mjs` boots after moving the isolated coordination home outside its fake Pi CLI root, but remains failed at the existing optional-feature batch HTTP timeout (line 1399). This does **not** establish a clean integrated suite. Earlier parent baseline reported five other full-suite Windows failures; full suite/check not rerun here. Browser specs remain unrun: Playwright CLI unavailable, no install authorized.
- `git diff --check` (unstaged) passes. `git diff --cached --check` reports an **existing staged** runner line-59 whitespace issue; worktree corrects it without staging. Current git index already contains many files staged by prior integrations, so `noStagedFiles=false` despite no staging in this continuation.

## Retained fixtures and risks

- Several diagnostic temp roots were deliberately retained after uncertain launcher evidence (`pi-webui-native-lifecycle-jK4vBo`, `ELZnOB`, `amA1mW`, `BXYNgI`, `vqhEKh`) and after the early interrupted shell-survival probe (`pi-native-shell-survival-zDfhON`). Do not replay or delete any uncertain job or force-release its fence. Later terminal-failed and known-success fixture roots may also remain due conservative fixture cleanup/Windows busy handles. No production coordination records were touched or cleared.
- Linux Bash actual crash-survival and real npm/Pi update commands remain unrun; all successful mutation evidence is fake CLI in disposable installations. On Windows a guardian spawn is not inferred as command completion: absent runner receipt still transitions to unknown/manual recovery. Arbitrary lifecycle-script secret output and deliberately detached script daemons remain approved limitations. Startup migration eligible-candidate and server wrapper ordering are code-reviewed but lack a dedicated full server behavioral regression; parent review should explicitly assess this gap before release.
- No acceptance claim for browser or whole suite; parent review required.
