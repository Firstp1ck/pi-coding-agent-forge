# Evaluation separation

A deterministic runtime fixture checks a mechanism. An independent outcome checks whether a task succeeded. A live run records a named model configuration. These are separate evidence classes.

Use the same frozen suite for baseline, current extension, isolated feature, and integrated guarded comparisons. Report separately authorized assistance independently. If no authorized adapter exists, record zero calls and an unavailable result; do not infer model quality.
