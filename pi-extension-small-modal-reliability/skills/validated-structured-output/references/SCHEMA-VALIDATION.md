# Declarative contract boundary

Allowed contract formats are JSON, CSV, enum, bounded string, and Markdown checklist. Contracts describe a finite expected shape only.

Do not include executable code, regular expressions, remote references, template expansion, commands, shell syntax, or YAML aliases. Structural validation is intentionally distinct from factual or behavioral verification.
